---
phase: 05-demo-submit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/scenario.md
  - demo/stage_anomaly.sh
  - demo/cleanup_demo.sh
autonomous: true
user_setup:
  - service: azure
    why: "Provision/operate demo GPU VM and stage cost anomaly"
    env_vars:
      - name: AZURE_SUBSCRIPTION_ID
        source: "Azure Portal -> Subscriptions"
    dashboard_config:
      - task: "Create a GPU VM (or pick existing) for the staged scenario"
        location: "Azure Portal -> Virtual Machines"

must_haves:
  truths:
    - "Demo scenario is reproducible: cost spike cause can be staged and later cleaned up"
    - "Demo scripts do not leak credentials and are safe to re-run"
  artifacts:
    - path: "demo/scenario.md"
      provides: "Step-by-step demo scenario"
    - path: "demo/stage_anomaly.sh"
      provides: "Scripted staging (best-effort)"
  key_links:
    - from: "demo/scenario.md"
      to: "demo/stage_anomaly.sh"
      via: "script referenced"
      pattern: "stage_anomaly\\.sh"
---

<objective>
Create the demo scenario documentation and scripts to stage/cleanup the cost anomaly.

Purpose: Make the hackathon demo repeatable on demand.
Output: `demo/scenario.md` and optional Azure CLI scripts for staging.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write reproducible demo scenario doc</name>
  <files>demo/scenario.md</files>
  <action>
Create `demo/scenario.md` describing:
- Baseline cost expectation
- What is staged (GPU VM left running, no auto-shutdown)
- How to trigger the alert webhook (real Azure alert or manual POST)
- Expected multi-agent outputs (cost driver, config, similar incident, diagnosis, remediation)
- Approval step and expected execution outcome
Keep it concise and aligned with the 2-minute script.
  </action>
  <verify>python3 -c "import pathlib; p=pathlib.Path('demo/scenario.md'); assert p.exists() and p.stat().st_size>200"</verify>
  <done>Scenario doc exists and includes setup, trigger, and expected outputs.</done>
</task>

<task type="auto">
  <name>Task 2: Add stage and cleanup scripts (best-effort, Azure CLI)</name>
  <files>demo/stage_anomaly.sh, demo/cleanup_demo.sh</files>
  <action>
Create two scripts:
- `demo/stage_anomaly.sh`: uses `az` to ensure the demo VM is running and tags it for identification (do not create expensive resources by default; require explicit `--vm-name` and `--resource-group`).
- `demo/cleanup_demo.sh`: stops the VM and removes demo tags.

Scripts must:
- refuse to run if required args missing
- print the exact `az` commands they will run
- never echo secrets
  </action>
  <verify>bash -n demo/stage_anomaly.sh && bash -n demo/cleanup_demo.sh</verify>
  <done>Scripts exist, are shellcheck-friendly, and are safe to re-run.</done>
</task>

</tasks>

<verification>
- `bash -n` passes for scripts
- (If Azure CLI logged in) stage + cleanup complete without errors
</verification>

<success_criteria>
- DEMO-01 met: scenario is documented and can be staged/cleaned up reliably
</success_criteria>

<output>
After completion, create `.planning/phases/05-demo-submit/05-01-SUMMARY.md`
</output>
