---
phase: 05-demo-submit
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - demo/script.md
  - demo/recording_checklist.md
autonomous: true
user_setup:
  - service: recording
    why: "Create the required 2-minute demo video"
    dashboard_config:
      - task: "Record screen + voiceover and export as MP4 under 2 minutes"
        location: "Local machine / any screen recorder"

must_haves:
  truths:
    - "A timecoded demo script exists and matches the staged scenario and system outputs"
    - "A recording checklist exists to produce the 2-minute end-to-end video"
  artifacts:
    - path: "demo/script.md"
      provides: "Timecoded demo script"
    - path: "demo/recording_checklist.md"
      provides: "Recording checklist (video is recorded manually later)"
  key_links:
    - from: "demo/script.md"
      to: "demo/recording_checklist.md"
      via: "recording checklist"
      pattern: "0:"
---

<objective>
Prepare the 2-minute demo script and a recording checklist.

Purpose: Satisfy hackathon deliverable DEMO-02 with clear, repeatable recording instructions.
Output: `demo/script.md` and `demo/recording_checklist.md` (video recorded manually later).
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/phases/05-demo-submit/05-01-SUMMARY.md
@.planning/phases/05-demo-submit/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write a timecoded 2-minute demo script + checklist</name>
  <files>demo/script.md</files>
  <action>
Create `demo/script.md`:
- Timecodes in 10-20s chunks
- Exact screens/commands to show (`curl` trigger, logs, Slack message, approval click)
- Expected one-liners for each agent output
  </action>
  <verify>python3 -c "import pathlib; p=pathlib.Path('demo/script.md'); assert p.exists() and '0:00' in p.read_text()"</verify>
  <done>Script exists and includes timecodes and expected outputs.</done>
</task>

<task type="auto">
  <name>Task 2: Add a recording checklist for the 2-minute video (manual step, non-blocking)</name>
  <files>demo/recording_checklist.md</files>
  <action>
Create `demo/recording_checklist.md`:
- Pre-flight checklist (Azure creds, Slack app/webhook, staged anomaly, terminal font size)
- Exact commands to run in order (stage -> run server -> trigger -> approve)
- What the recording must show (trigger -> parallel agents -> diagnosis -> Slack approval -> remediation outcome)
- Export requirements: MP4, target ~2:00, file path `demo/video.mp4`

This plan should NOT block on the actual recording; the checklist makes recording fast and repeatable.
  </action>
  <verify>python3 -c "import pathlib; p=pathlib.Path('demo/recording_checklist.md'); assert p.exists() and 'demo/video.mp4' in p.read_text()"</verify>
  <done>Recording checklist exists and is actionable.</done>
</task>

</tasks>

<verification>
- Demo script and recording checklist exist and are consistent
</verification>

<success_criteria>
- Ready to record DEMO-02 quickly: script + checklist exist (recording itself is manual)
</success_criteria>

<output>
After completion, create `.planning/phases/05-demo-submit/05-03-SUMMARY.md`
</output>
