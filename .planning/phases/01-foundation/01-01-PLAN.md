---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - requirements-dev.txt
  - .env.example
  - src/web/app.py
  - src/web/settings.py
autonomous: true

must_haves:
  truths:
    - "Repo has a runnable Python app entrypoint"
    - "A local dev can start a webhook receiver server"
    - "Core package imports resolve with PYTHONPATH=src"
  artifacts:
    - path: "src/web/app.py"
      provides: "FastAPI app with /health"
    - path: "requirements.txt"
      provides: "Runtime dependencies"
---

<objective>
Create the minimal project scaffold needed to run and test the system locally.

Purpose: Make every later agent/SDK task incremental (not blocked by missing structure).
Output: Python package layout and a basic web server.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Python project scaffolding (src/, tests/, scripts/)</name>
  <files>requirements.txt, requirements-dev.txt, src/agents/__init__.py, src/models/__init__.py, src/storage/__init__.py, src/web/__init__.py, src/llm/__init__.py, src/integrations/__init__.py, src/execution/__init__.py</files>
  <action>
Create a minimal Python layout that matches the roadmap deliverables (`src/agents`, `src/models`, etc.):
- Create package folders with `__init__.py`: `src/agents`, `src/models`, `src/storage`, `src/web`, `src/llm`, `src/integrations`, `src/execution`.
- Create the `src/azure/` folder but DO NOT add `src/azure/__init__.py`.
  Rationale: Azure SDK packages live under the `azure.*` namespace; adding `src/azure/__init__.py` would shadow the SDK and break imports like `azure.identity`.
- Add `requirements.txt` with: `fastapi`, `uvicorn[standard]`, `pydantic`, `python-dotenv`, `httpx`.
- Add `requirements-dev.txt` with: `pytest`, `ruff`.
Keep it boring and hackathon-friendly (no Poetry/uv lockfiles unless already present).
  </action>
  <verify>python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt -r requirements-dev.txt && PYTHONPATH=src python3 -c "import agents, models, storage, web"</verify>
  <done>Imports resolve for the core packages with `PYTHONPATH=src`.</done>
</task>

<task type="auto">
  <name>Task 2: Add a minimal webhook receiver FastAPI app</name>
  <files>src/web/app.py, src/web/settings.py, .env.example</files>
  <action>
Implement a minimal FastAPI app:
- `GET /health` returns `{ "ok": true }`.
- `POST /webhooks/alert` accepts JSON (any shape for now), logs it, and returns `{ "received": true }`.
- Add `src/web/settings.py` that loads env vars via `python-dotenv` (do not require env vars yet; defaults must allow server start).
- Create `.env.example` with placeholders used later (Azure + Slack), but do not fail startup if missing.
  </action>
  <verify>. .venv/bin/activate && PYTHONPATH=src python3 -c "from web.app import app; assert app"</verify>
  <done>Local server code exists; importing `web.app:app` succeeds without environment configuration.</done>
</task>

</tasks>

<verification>
- `. .venv/bin/activate && PYTHONPATH=src python3 -c "from web.app import app; assert app"`
</verification>

<success_criteria>
- Repo has a runnable FastAPI webhook receiver
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
